<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AnotherWeatherApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AnotherWeatherApp" parent="Base.Theme.AnotherWeatherApp" />

    <style name="TextAppearance.Custom" parent="TextAppearance.MaterialComponents.Body2">
        <item name="android:textColor">@color/white</item>
        <item name="android:shadowColor">@color/shadow_color</item>
        <item name="android:shadowDy">4</item>
        <item name="android:shadowDx">4</item>
        <item name="android:shadowRadius">2</item>
    </style>

    <style name="Widget.MaterialComponents.Button.Custom" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:layout_width">150dp</item>
        <item name="android:layout_marginBottom">16dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/button_background</item>
        <item name="backgroundTint">@null</item>
        <item name="iconTint">@color/white</item>
        <item name="android:textAppearance">@style/TextAppearance.Custom</item>
    </style>
</resources>